# 維護指南

改東西、推上線、出事怎麼救。給第一次接手的人看的。

---

## 目錄

- [一、把專案拿下來](#一把專案拿下來)
- [⚠️ 這台開發機有影子衛士](#️-這台開發機有影子衛士)
- [二、改東西的完整流程](#二改東西的完整流程)
- [三、推上 GitHub](#三推上-github)
- [四、常見狀況排除](#四常見狀況排除)
- [五、出事了怎麼救](#五出事了怎麼救)
- [六、改程式前一定要知道的事](#六改程式前一定要知道的事)

---

## 一、把專案拿下來

> ⚠️ **不要依賴任何人電腦上的本機資料夾。**
> 重開機會清空所有變更，本機檔案隨時可能消失。
> **GitHub 上的才是唯一的正本。**

第一次接手先確認工具在：

```bash
git --version      # 要有 git
python --version   # 用來起本機伺服器（或用 node）
```

然後從 GitHub 抓下來：

```bash
cd 你想放專案的資料夾
git clone https://github.com/qiyi-studio/company-events.git
cd company-events
```

**線上網址**：<https://qiyi-studio.github.io/company-events/>

**GitHub 倉庫**：<https://github.com/qiyi-studio/company-events>

**Firebase 專案**：`company-d826e`（[主控台](https://console.firebase.google.com/)）

---

## ⚠️ 電腦會重置

**重開機會清空所有本機變更。** 所以：

- **每次改完一定要 `git push`** —— 沒推上去的東西重開就永久消失
- 收工前跑這兩行確認都推上去了：

```bash
git status                            # 要看到 "nothing to commit, working tree clean"
git log origin/main..HEAD --oneline   # 沒有任何輸出 = 都推上去了
```

- **不要把本機資料夾當備份。** GitHub 上的才是正本，本機隨時會沒。

---

## 二、改東西的完整流程

### 1. 先在本機跑起來

```bash
cd company-events
python -m http.server 8000
```

瀏覽器開 <http://localhost:8000>

> ⚠️ **絕對不要直接雙擊 `index.html`**。用 `file://` 開啟時 Firebase 登入會失敗，
> 你會以為程式壞了，其實只是開啟方式不對。一定要走 `localhost`。

停止伺服器：在終端機按 `Ctrl + C`

### 2. 改 `index.html`

整個網站就是這一支檔案，沒有 build、沒有套件。用任何編輯器打開改就好。

檔案結構大致是：

| 區段 | 大約位置 | 內容 |
|---|---|---|
| `<style>` | 前 1/5 | 所有 CSS |
| `<body>` | 中間 | HTML 結構（大廳、遊戲區、各種彈窗） |
| `<script type="module">` | 後 3/4 | 全部邏輯 |

### 3. 改完一定要更新版本號

在 `index.html` 裡找到：

```js
const APP_VERSION = "v32.0.0";
```

**改完功能就把它加一號。** 這件事看起來多餘，但它是你之後唯一能確認「線上到底跑的是不是新版」的方法 —— 版本號會顯示在網頁左上角招牌旁邊。

沒更新版本號，之後遇到問題你會分不清是程式有 bug 還是瀏覽器拿到舊快取，白白浪費很多時間。

### 4. 本機測過再推

重新整理 `localhost:8000`，確認：

- 左上角版本號是你剛改的
- 能取暱稱進大廳
- 能建房間、進房間
- 你改的那個功能真的正常

**多人功能一定要多開幾個視窗測**，見下方「測試多人」。

---

## 三、推上 GitHub

```bash
cd company-events
node check.mjs          # 先跑自我檢查，有 ✗ 就別推
git add -A
git commit -m "簡短說明你改了什麼"
git push
```

推完之後：

1. 到 <https://github.com/qiyi-studio/company-events/actions> 看最上面那筆是不是**綠色勾勾**（黃色圓點 = 還在建置，等它跑完，通常 1–2 分鐘）
2. 開 <https://qiyi-studio.github.io/company-events/>
3. **按 `Ctrl + Shift + R` 強制重新整理**（一般的 F5 常常拿到舊快取）
4. 確認左上角版本號是新的

> ⚠️ **所有人都要重新整理。** 如果有人的分頁還跑舊版，可能會用舊邏輯寫資料庫，
> 造成只有部分人正常的詭異狀況。開新局前先請大家 `Ctrl+Shift+R`。

---

## 四、常見狀況排除

### 網站打不開，顯示 404

Pages 還在建置，或建置失敗。到 Actions 頁面看狀態。

### 打得開但跳「登入 Firebase 失敗」

Firebase 那邊的問題，依序檢查：

1. **Authentication → 登入方式 → 匿名** 是不是「已啟用」
2. **Authentication → 設定 → 已授權的網域** 有沒有 `qiyi-studio.github.io`
   （只填網域，不要帶 `https://`，也不要帶路徑）

### 能進大廳但建不了房間

**Realtime Database → 規則**應該是：

```json
{
  "rules": {
    "rooms": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}
```

改完要按「**發布**」才會生效。

### 改了程式但線上沒變

九成是快取。依序做：

1. 確認 `git push` 真的成功（`git status` 應該顯示 `nothing to commit`）
2. Actions 頁面確認建置完成（綠勾）
3. `Ctrl + Shift + R` 強制重新整理
4. **看左上角版本號** —— 這是唯一可靠的判斷依據

### 遊戲卡住不動

先看是誰的回合。如果是離線的人：

- 系統會在 **12 秒後自動跳過**（由房主的瀏覽器負責執行，所以**房主的分頁必須開著**）
- 房主也可以直接按中央的「跳過此玩家（離線）」不用等

如果房主自己也關掉了，房主權限會在 5 秒後自動移轉給還在線上的人，由新房主接手。

### 有個房間刪不掉

進去那個房間等 5 秒，你會自動變成房主（原房主離線超過 5 秒就會移轉），然後左欄下方會出現「刪除房間」。

或者不管它 —— 沒人在線且超過 30 分鐘沒動靜的房間會自動被清掉。

---

## 五、出事了怎麼救

### 改壞了想退回上一版

```bash
git log --oneline          # 找到想退回的那個 commit 編號
git revert <commit編號>     # 產生一個「反向修改」的新 commit
git push
```

`revert` 比 `reset` 安全 —— 它不會刪掉歷史，只是新增一筆把改動抵銷掉的紀錄。

### 只是想丟掉還沒 commit 的修改

```bash
git checkout -- index.html   # 把 index.html 還原成上次 commit 的樣子
```

⚠️ 這會**直接丟掉**你的修改，救不回來。確定不要了再執行。

### 資料庫被搞亂了

Firebase 主控台 → **Realtime Database** → **資料**分頁 → 找到 `rooms` → 點右邊的 **⋮** → 刪除。

這只會清掉房間資料，不影響程式碼。大家重新整理後就是乾淨的大廳。

### 完全不知道發生什麼事

按 `F12` 打開開發者工具 → **Console** 分頁 → 看有沒有紅字。紅字通常會直接告訴你哪一行出錯。

---

## 六、改程式前一定要知道的事

這些是踩過坑之後留下的紀錄，改到相關的地方請先看一眼。

### 測試多人：同一個瀏覽器的分頁算同一個人

玩家身分存在 `localStorage`，**整個瀏覽器共用**。開三個分頁進同一間房，系統只會看到一個玩家。

要模擬三個人：

| 玩家 | 怎麼開 |
|---|---|
| A | Chrome 一般視窗 |
| B | Chrome 無痕視窗（`Ctrl+Shift+N`） |
| C | Edge 或 Firefox |

無痕視窗彼此共用，所以**只能算一個玩家**。

⚠️ **不要用無痕視窗當房主** —— 關掉之後身分永久消失，那個房間就沒人能刪了。

### 寫入資料庫請用「一次寫完」

Firebase 的多路徑 `update()` 是原子的，**同一件事的所有欄位一定要放在同一次 `update()`**。

拆成兩次寫入會露出「中間狀態」，其他人的瀏覽器會看到一個不該存在的畫面組合，然後做出錯誤反應。這個專案已經因為這個原因出過兩次 bug（賓果加賽打不完、已淘汰的人還能叫號）。

如果需要「只讓一個人負責寫入」，用 `runTransaction` 搶一個**沒有任何監聽器的路徑**當發動權（例如 `playoffClaim`），這樣搶發動權的動作不會被別人看見。

### transaction 的 callback 可能被執行很多次

```js
runTransaction(ref, (現在的值) => { ... 回傳新的值 ... })
```

這個 callback **會被重複呼叫**（本地快取跑一次、伺服器回來再跑一次）。所以：

- **不要**在 callback 裡面改外面的變數，然後在外面用那個變數 —— 會拿到被丟棄那次的結果
- 要用結果請讀 `res.snapshot.val()`，那才是真正寫進去的值

### 不要用本機變數判斷「這件事做過了嗎」

本機變數在使用者重新整理後就沒了。曾經有個 bug：積分用本機變數去重，結果房主按「積分歸零」之後，任何人重新整理都會把分數重新寫回去。

要記錄「已經處理過」請寫進資料庫（這個專案用 `meta/scoredRounds/{局次}`）。

### 這些狀態切換房間時一定要清乾淨

`subscribeRoom()` 裡的 `resetRoomLocalState()` 負責這件事。**新增任何「屬於某一間房」的模組層級變數時，記得加進去。**

漏掉會發生跨房污染 —— 曾經有個 bug 是帶著 A 房的號碼卡進 B 房，直接把 B 房那一局終結掉。

### 資料庫裡的東西所有人都看得到

`secret`（終極密碼的答案）、已掀開的杯子內容，都存在每個人的瀏覽器裡。畫面上的「保密」只是 UI 遮蔽，開 DevTools 就看得到。

純前端 + Firebase 的架構沒辦法真正隱藏已寫入的資料。**不要拿這個工具處理任何敏感資訊。**

### 改完請跑一次自我檢查

```bash
node check.mjs
```

會檢查四件事：

| 檢查 | 為什麼重要 |
|---|---|
| JavaScript 語法 | 有錯的話整個網站一片空白 |
| `$("...")` 抓的 id 是否都存在 | **打錯一個 id 就會讓程式在載入時直接掛掉** |
| 有沒有重複的 id | 會抓到錯的元素，行為詭異又難查 |
| 版本號 | 提醒你改完要加一號 |
| 房間狀態變數有沒有漏清 | 漏了會造成跨房污染（見上面那條） |

輸出「✓ 全部通過」才推上線。有 ✗ 就先修好。

### 還有一組邏輯測試

```bash
node tests/run-all.mjs
```

它把 `index.html` 裡的函式**抽出來實際執行**（不是另外抄一份），所以改壞了會真的被抓到。
目前有 126 項驗證，涵蓋賽程推進、積分預算、加賽、離線處理、六種遊戲的規則計算。

**改到遊戲規則、積分、加賽、輪次這些地方時一定要跑。**

> ⚠️ 這兩支腳本都只驗證**邏輯**，不會測到「多人同時操作」的真實時序問題 ——
> 這個專案大部分的嚴重 bug 都是那類。功能還是要自己開三個瀏覽器實際玩過。

---

## 附錄：資料庫結構

```
rooms/{房間ID}/
├── meta/                    房間設定與狀態
│   ├── name, hostId, hasPassword, passwordHash
│   ├── gameState            lobby → preparing → playing → finished
│   ├── schedule[]           賽程 [{type, rounds, points}]
│   ├── scheduleIndex        目前第幾個遊戲
│   ├── roundInStage         該遊戲第幾局
│   ├── scoreCycle           賽程跑完第幾圈（保留積分時用）
│   ├── scoredRounds/{局次}   已結算標記（保證每局只發一次分）
│   ├── turnIndex, turnStart 輪次；turnStart 每局 +1 讓先手輪替
│   ├── finalists[]          加賽時還在場的人
│   ├── winners/{uid}        本局勝者
│   └── 各遊戲的規則設定
├── players/{uid}            name, online, order, ready, brag
├── boards/{uid}/numbers     賓果號碼卡
├── calledNumbers/{號碼}      賓果已叫號碼
├── pwState                  終極密碼 {lo, hi, secret, hitBy}
├── pwGuesses                終極密碼的猜測紀錄
├── cardState                牌局 {hands, table, picks, stood, shown}
├── lotteryState             抽獎 {total, prizes, picks, skipped}
├── scores/{局次}/{uid}       積分
├── playoffClaim             加賽發動權（刻意不被監聽）
└── logs                     遊戲日誌
```
